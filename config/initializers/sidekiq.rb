# Sidekiq.hook_rails!  # uncomment if you want the delay extensions, but prefixed with `sidekiq_`
#Sidekiq.remove_delay! # You can disable them in a Rails application by calling

# Sidekiq.configure_server do |config|
#   config.redis = { url: 'redis://localhost:9999/0' }
# end

# Sidekiq.configure_client do |config|
#   config.redis = { url: 'redis://localhost:9999/0' }
# end

# config.average_scheduled_poll_interval = 15 #15 is in sec

